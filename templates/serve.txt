apiVersion: ai.sap.com/v1alpha1
kind: ServingTemplate
metadata:
  name: cicdexampleserving
  annotations:
    scenarios.ai.sap.com/description: "cicdexampleserving"
    scenarios.ai.sap.com/name: "cicdexampleserving"
    executables.ai.sap.com/description: "cicdexampleserving"
    executables.ai.sap.com/name: "cicdexampleserving"
  labels:
    scenarios.ai.sap.com/id: "cicdexampleserving"
    ai.sap.com/version: "1.0"
spec:
  inputs:
    parameters: 
       - name: env_example
         default: value1
         type: string
         description: description of the parameter
  template:
    apiVersion: "serving.kserve.io/v1beta1"
    metadata:
      annotations: |
        autoscaling.knative.dev/metric: concurrency
        autoscaling.knative.dev/target: 1
        autoscaling.knative.dev/targetBurstCapacity: 0
      labels: |
        ai.sap.com/resourcePlan: basic
    spec: |
      predictor:
        imagePullSecrets:
          - name: felixdockersecrect
        minReplicas: 1
        maxReplicas: 5
        containers:
        - name: kserve-container
          image: docker.io/bfwork/cicdexample
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            - name: ENV_EXAMPLE
              value: "{{inputs.parameters.env_example}}"